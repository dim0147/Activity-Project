@{
    ViewBag.Title = "Club Detail";
    ViewBag.BCHeader = "Club Detail";
    ViewBag.BCAction = "";
    ViewBag.BCController = "";
    ViewBag.BCActionText = "";
}

@section css {
    <link rel="stylesheet" href="~/Content/css/Shared/ckeditor.css" />
}

@Html.Partial("_Breadcrumb")

@Html.AntiForgeryToken()

@Html.Hidden("__ClubId", (int)ViewBag.ClubId)

<div id="root">

</div>


@section Scripts{
    <script async charset="utf-8" src="//cdn.embedly.com/widgets/platform.js"></script>
    <script src="~/Scripts/jquery.signalR-2.2.2.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>

    <script>
        // Reference the auto-generated proxy for the hub.
        @*const chat = $.connection.chatHub;
        // Create a function that the hub can call back to display messages.
        chat.client.ReceiveMessage = function (message) {
            // Add the message to the page.
            console.log("Receive message");
            console.log(message);
        };

        // Start the connection.
        $.connection.hub.start().done(function () {
            console.log("Connect success");
            // Call the Send method on the hub.
            chat.server.sendMess({ User: 'cc', Message: 'noob'});
        });*@

    </script>
    @Scripts.Render("~/dist/Club/Detail.js")
}